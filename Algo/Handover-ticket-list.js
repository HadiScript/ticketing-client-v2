import React from "react";

// same as picked tiket

const HandoverTicketList = () => {
  // ** first step
  // authContext
  // loading state
  // list state

  // ** second step
  // getting all handover ticket
  // url: http://localhost:5000/api/handover-to-me -> Get
  // call in useEFfect lifecycle -> auth && auth?.token call getting all handover ticket

  // ** third step
  // render list

  return <div>HandoverTicketList</div>;
};

export default Handover - ticket - list;
